# Dependências diretas do projeto, pinadas nas versões realmente testadas
# (ambiente de referência: Python 3.13.3, Windows).

# Análise de dados
pandas==2.3.3
numpy==2.5.1
pyarrow==24.0.0

# Notebooks e gráficos
jupyter==1.1.1
matplotlib==3.11.1

# Painel
streamlit==1.60.0
plotly==6.9.0

# Coleta no DATASUS (só para reprocessar dados; os parquets já vêm no repo)
pysus==2.7.0
pyreaddbc==2.0.4
